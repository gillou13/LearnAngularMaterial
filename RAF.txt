Liste des choses a faire : 
    - DialogService : service de boite de dialoge.
        - OpenDiagYesNo
        - OpenDiagYesNoCancel
    - StateTabService : gestion de l'état des composants liés au tabs.
        - enregisement de l'état (formChange, recherche, encrage)
        ? a integer dans navigation.service.ts ?
    - navigationService
        ? enregistrer la navigation en sessionStorage (F5 sans perdre la navigation) ?
        - remplacer la fonction createLink d
    - Tableaux :    
EC        - comme order line.
    - formulaire
        - tester les inputs 'classique'
OK        - trouver solution pour le input number.
        - tester les mat-form-field.
    - theming
        - global
        - composant
