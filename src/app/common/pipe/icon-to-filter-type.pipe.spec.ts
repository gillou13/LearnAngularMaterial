import { IconToFilterTypePipe } from './icon-to-filter-type.pipe';

describe('IconToFilterTypePipe', () => {
  it('create an instance', () => {
    const pipe = new IconToFilterTypePipe();
    expect(pipe).toBeTruthy();
  });
});
